name: Fastlane Deploy

on:
  workflow_dispatch:
    inputs:
      track:
        description: "Play track (internal | closed | production)"
        required: true
        default: "internal"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Ruby (with Bundler cache)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler-cache: true
          working-directory: android   # ⭐ مهم

      - name: Write Google Play service account JSON
        run: |
          echo '${{ secrets.GOOGLE_PLAY_JSON_KEY }}' > android/play-key.json

      - name: Install dependencies
        working-directory: android     # ⭐ مهم
        run: bundle install

      - name: Deploy to Play (Fastlane)
        working-directory: android     # ⭐ مهم
        env:
          GOOGLE_PLAY_JSON_KEY_PATH: play-key.json
          TRACK: ${{ inputs.track }}
        run: bundle exec fastlane android deploy --env "${TRACK}"
